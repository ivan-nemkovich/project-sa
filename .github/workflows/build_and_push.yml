name: Build and Publish image

on:
  push:
    branches:
      - main
    tags:
      - v**
jobs:
  push_to_registries:
    name: Push Docker image
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      #- name: Get tag
      #  id: tag
      #  uses: dawidd6/action-get-tag@v1
       # with:
        # Optionally strip `v` prefix
        #  strip_v: true

      - name: Check out the repo
        uses: actions/checkout@v2.4.2
        with:
          ref: main
     #     fetch-depth: 0
          token: ${{ secrets.CR_TOKEN }}

      #- name: Log in to the Container registry
      #  uses: docker/login-action@v2
      #  with:
      #    registry: ghcr.io
      #    username: ${{ github.actor }}
      #    password: ${{ secrets.CR_TOKEN }}

      #- name: Build and push Docker images
      #  uses: docker/build-push-action@v3
      #  with:
      #    context: ./image
      #    push: true
      #    tags: ghcr.io/ivan-nemkovich/wp_project:latest,ghcr.io/ivan-nemkovich/wp_project:${{ steps.tag.outputs.tag }} 

      #- name: Helm tool installer
      #  uses: Azure/setup-helm@v3
      
      #- name: Helm Package
      #  run: |
      #    cd chart
      #    helm package wp_project --version=${{ steps.tag.outputs.tag }} --app-version=${{ steps.tag.outputs.tag }}
      #    cd ..
      #    helm repo index --url https://ivan-nemkovich.github.io/project-sa/ --merge index.yaml .
      #    sed -i 's/targetRevision:.*/targetRevision: ${{ steps.tag.outputs.tag }}/' argocd-apps/templates/wordpress.yml

      - name: validate_manifest
        id: validation
        uses: makocchi-git/actions-k8s-manifests-validate-kubeval@v1.0.1
        with:
          files: argocd-apps/templates/wordpress.yml
      
      - name: Send GitHub Action trigger data to Slack workflow
        if: always()
        id: slack
        uses: slackapi/slack-github-action@v1.19.0
        with:
          payload: |
            {
              "text": "GitHub Action validation result: ${{ steps.validation.outcome }}\n${{ github.event.pull_request.html_url || github.event.head_commit.url }}",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "GitHub Action validation result: ${{ steps.validation.outcome }}\n${{ github.event.pull_request.html_url || github.event.head_commit.url }}"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      #- name: Commit and push changes
      #  run: |
      #    git config user.name github-actions
      #    git config user.email github-actions@github.com
      #    git add .
      #    git commit -m "Generated by GitHub Action. ${{ steps.tag.outputs.tag }}"
      #    git push origin main
